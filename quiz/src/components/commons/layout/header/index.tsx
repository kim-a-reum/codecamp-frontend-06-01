import styled from "@emotion/styled";

const Wrapper = styled.div`
  background-color: #c0441b;
  height: 70px;
`;
export default function LayoutHeader() {
  return <Wrapper> 여기는 header영역 입니다 </Wrapper>;
}
