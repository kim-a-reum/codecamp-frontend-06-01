import styled from "@emotion/styled";

const Wrapper = styled.div`
  height: 500px;
  width: 500px;
  background-color: #87bbcf;
`;

export default function LayoutSidebar() {
  return <Wrapper> 여기는 sidebar영역 입니다 </Wrapper>;
}
